### Setting up a Descartes Lab VM 

#### First activate anaconda within the shell by running 
`exec $SHELL`

####  Then pip install the correct pytorch versions
`pip install torch==1.9.0 torchvision==0.10.0`
